-- [アフィン変換] --
    - x -> ax + b とする変換のこと．

    - p_i <-> (a_i,b_i)  を x -> a_ix + b により対応させる．(ここでa_i,b_i>=1,x>=1)
    p_iらの順番を変えて，順にxにアフィン変換を施す時，変換後のxが最小となるような変換の順番は，
        [まず a_i>=2 に対してb_i/(a_i -1) の昇順に作用させ，最後にa_i = 1の場合を作用させる]
    (隣り合うニ変換のswapがどのような場合に施されるべきかを考える)

    - アフィン変換をセグ木に載せる方法







-- [Hadamard変換] -- 
    - フーリエ変換の一般化の一つ
    
